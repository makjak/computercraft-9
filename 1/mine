turtle.digDown()
turtle.placeUp()
while turtle.down() do
  turtle.digDown()
  t = 4
  while t > 0 do
    turtle.dig()
    turtle.turnRight()
    t = t - 1
  end
end

while not(turtle.detectUp()) do
  turtle.up()
end

turtle.digUp()
turtle.placeDown()
write(turtle.getFuelLevel())
